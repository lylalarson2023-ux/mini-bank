<nav class="nav-list">
    <div class="nav-item">
        <NavLink class="nav-link-custom" href="" Match="NavLinkMatch.All">
            <i class="bi bi-house-door-fill"></i> DASHBOARD
        </NavLink>
    </div>

    @* Module Compte courant supprimé (refonte UI, doublon du dashboard) —
       le retrait vit sur /retrait, accessible via l'action rapide du dashboard. *@
    <div class="nav-item">
        <NavLink class="nav-link-custom" href="epargne">
            <i class="bi bi-piggy-bank"></i> EPARGNE PRO
        </NavLink>
    </div>

    @* ─── Modules retirés temporairement du MVP (Tuteur, Crédit) ───
       Réintégration : décommenter ces liens + réactiver les @page des
       pages correspondantes. Le code métier est intact (tag v1-5-modules-sqlite).
    <div class="nav-item">
        <NavLink class="nav-link-custom" href="tuteur-dashboard">
            <i class="bi bi-shield-lock"></i> ESPACE TUTEUR
        </NavLink>
    </div>

    <div class="nav-item">
        <NavLink class="nav-link-custom" href="credits">
            <i class="bi bi-cash-stack"></i> CREDITS
        </NavLink>
    </div>
    *@

    <div class="nav-item">
        <NavLink class="nav-link-custom" href="historique">
            <i class="bi bi-list-ul"></i> HISTORIQUE
        </NavLink>
    </div>

    <div class="nav-item">
        <NavLink class="nav-link-custom" href="profil">
            <i class="bi bi-person-badge"></i> MON PROFIL
        </NavLink>
    </div>

    @* Notifications : la page détachée a été intégrée à Paramètres (onglet
       Notifications) — la cloche du header reste le point d'accès rapide. *@
    <div class="nav-item">
        <NavLink class="nav-link-custom" href="parametres">
            <i class="bi bi-gear"></i> PARAMETRES
        </NavLink>
    </div>

    <AuthorizeView Roles="Admin">
        <Authorized>
            <div class="nav-item">
                <NavLink class="nav-link-custom" href="admin">
                    <i class="bi bi-terminal-fill"></i> ADMIN TERMINAL
                </NavLink>
            </div>
        </Authorized>
    </AuthorizeView>
</nav>
